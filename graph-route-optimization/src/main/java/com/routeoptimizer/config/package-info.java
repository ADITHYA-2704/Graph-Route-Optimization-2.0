/**
 * Configuration package.
 * <p>
 * Holds classes responsible for reading configuration (database URL, credentials,
 * driver settings, application-level constants) from properties files and exposing
 * them to the rest of the application. This is the only package that should read
 * {@code application.properties} / {@code db.properties} directly.
 */
package com.routeoptimizer.config;