/**
 * Domain model package.
 * <p>
 * Contains plain-old-Java-objects (POJOs) that represent the core domain concepts
 * of the system: locations/intersections (nodes), roads/connections (edges), and
 * computed routes. These classes hold only data and simple accessors - no business
 * logic, no JDBC or algorithm code belongs here.
 */
package com.routeoptimizer.model;