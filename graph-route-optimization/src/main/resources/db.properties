server.port=8081

# -----------------------------------------------------------
# Spring Data JPA Datasource Configuration (Port 3307)
# -----------------------------------------------------------
spring.datasource.url=jdbc:mysql://127.0.0.1:3307/route_optimizer_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=adhi3961
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -----------------------------------------------------------
# Legacy DBConnection Configuration (Port 3307)
# -----------------------------------------------------------
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://127.0.0.1:3307/route_optimizer_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=adhi3961

# -----------------------------------------------------------
# JPA / Hibernate Configurations
# -----------------------------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect