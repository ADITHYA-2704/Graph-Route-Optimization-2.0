package com.routeoptimizer;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Placeholder smoke test, kept only to confirm the JUnit 5 / Maven Surefire
 * test toolchain itself is wired up correctly. Real tests for this project's
 * classes live alongside their production code under src/test/java, e.g.
 * GraphTest, DijkstraAlgorithmTest, NodeDAOTest, RouteOptimizationServiceTest,
 * ValidationUtilTest.
 */
public class AppTest {

    @Test
    void appLoads() {
        assertTrue(true);
    }
}